/* =========================================================
   Chakradhari — Updates data
   Public activity: talks, visits, workshops, milestones. Empty
   until something real happens — js/pages/updates.js renders
   nothing (not a "coming soon" state) when this is empty.

   Shape: { date: 'YYYY-MM-DD', title, location, description,
            images: [{ src, alt }], href? }
   ========================================================= */

export const updates = [];
