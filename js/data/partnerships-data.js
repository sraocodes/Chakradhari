/* =========================================================
   Chakradhari — Public collaborations data
   Only PUBLIC institutional relationships belong here — announced
   collaborations, signed/public MoUs, named funded programmes.
   Private/stealth engagements must never appear in this file.
   Empty until something is actually announced.

   Shape: { institution, purpose, date: 'YYYY-MM-DD', href?, image?: { src, alt } }
   ========================================================= */

export const collaborations = [];
