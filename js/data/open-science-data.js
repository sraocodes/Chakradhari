/* =========================================================
   Chakradhari — Open Science data
   No fake tools, repos, notebooks or channels. Populate these
   arrays as real, shippable work exists; js/pages/open-science.js
   renders nothing when they're empty rather than showing
   placeholder cards.

   Shapes (for when real entries are added):
   tools:    { title, domain, summary, links: [{ label, href }] }
   channels: { name, domain, description, href, videos?: [{ title, href, duration }] }
   ========================================================= */

export const tools = [];

export const channels = [];
